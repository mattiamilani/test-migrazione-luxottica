name: luxottica-secret-test
on:
  workflow_dispatch:
env:
  DOCKER_USER: mattiamilani
jobs:
  Login:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - name: echo message
      run: echo "Effettuo il login per ${{ env.DOCKER_USER }}..."
    - name: sh
      shell: bash
      run: echo 'Login in corso con password segreta...'
  Deploy:
    runs-on: ubuntu-latest
    needs: Login
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - name: sh
      shell: bash
      run: echo 'Push dell immagine su Docker Hub completato!'
